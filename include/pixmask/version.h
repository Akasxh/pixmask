#pragma once

namespace pixmask {

inline constexpr int version_major() noexcept { return 0; }
inline constexpr int version_minor() noexcept { return 1; }
inline constexpr int version_patch() noexcept { return 1; }

} // namespace pixmask
